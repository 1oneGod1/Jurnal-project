{
  "name": "jurnal-upload-api",
  "version": "1.0.0",
  "description": "Backend API untuk generate presigned URL Cloudflare R2 upload dokumentasi Jurnal Proyek Arduino",
  "main": "api/generate-upload-url.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "echo 'No build needed for Vercel serverless'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "cloudflare",
    "r2",
    "upload",
    "api",
    "presigned-url"
  ],
  "author": "Jurnal Project Team",
  "license": "MIT",
  "dependencies": {
    "@aws-sdk/client-s3": "^3.514.0",
    "@aws-sdk/s3-request-presigner": "^3.514.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
